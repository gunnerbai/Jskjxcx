<view class='container'>

  <view class='tab-wrapper'>
    <view id='tabright' class='tab-right {{currentTab === 0 ? "tab-active" : ""}}' bindtap='switchTab'>大咖秀</view>
    <view id='tableft' class='tab-left {{currentTab === 1 ? "tab-active":""}}' bindtap='switchTab'>美妆秀</view>
  </view>

  <view class='content-wrapper' wx:if='{{currentTab === 0}}'><music/></view>
  <view class='content-wrapper' wx:if='{{currentTab === 1}}'><movie/></view>

</view>